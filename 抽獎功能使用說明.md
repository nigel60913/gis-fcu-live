# 集團福利點數抽獎功能

## 操作方式

1. 先確認觀眾已完成前面題目的作答。
2. 在 `admin.html` 主持控制台按下「✦ 投影抽獎頁」。
3. 系統會直接在既有 `display.html` 內呈現完整抽獎畫面，不會開啟或載入 `lottery.html` 網址。
4. 在 Display 的完整抽獎介面確認可抽獎人數後，按下「開始抽獎」。
5. 畫面播放 5 秒抽獎動畫與答案公布音樂，之後一次公布最多 20 位得獎者。
6. 按下「匯出得獎名單 CSV」即可下載得獎序號、暱稱、完成題數、抽獎時間及識別碼。

## 抽選規則

- 資料來源：所有題目的 `responses` 子集合。
- 去重方式：優先使用回答文件的 `clientId`；只有舊資料缺少識別碼時，才以標準化暱稱去重。
- 測試資料：排除本專案 150 人壓測使用的 `loadtest_數字` 識別碼。
- 抽選方式：使用瀏覽器加密亂數執行 Fisher–Yates 隨機排序。
- 得獎名額：答題者達 20 位時抽出 20 位；不足 20 位時全部列為得獎者。
- 操作權限：只有已登入的授權主持人能從控制台切換 Display 至抽獎狀態。

## 點數說明

抽獎頁依據提供的福利點數說明呈現：

`集團點 1 點 = 小樹點 1 點 = 新臺幣 1 元`

抽獎頁僅以重點文字介紹福利點數概念，不嵌入或提供完整 PDF。

## 音效

Display 抽獎畫面使用答案公布的 `happy-upbeat-results.mp3` 作為背景音樂。瀏覽器依規定必須先在投影頁點擊「開啟音效」才能播放；可使用右下角控制調整音量或靜音。
