# 集團福利點數抽獎功能

## 操作方式

1. 先確認觀眾已完成前面題目的作答。
2. 在 `admin.html` 主持控制台按下「✦ 開啟抽獎頁 ↗」。
3. 系統會另開 `lottery.html`，並自動彙整所有題目的答題者。
4. 確認可抽獎人數後，按下「開始抽獎」。
5. 畫面播放 5 秒抽獎動畫與音樂，之後一次公布最多 20 位得獎者。
6. 按下「匯出得獎名單 CSV」即可下載得獎序號、暱稱、完成題數、抽獎時間及識別碼。

## 抽選規則

- 資料來源：所有題目的 `responses` 子集合。
- 去重方式：優先使用回答文件的 `clientId`（亦為 response document ID）；只有舊資料缺少識別碼時，才以 trim、空白正規化及不分大小寫的暱稱去重。同一人回答多題只保留一個抽獎資格。
- 測試資料：排除本專案 150 人壓測使用的 `loadtest_數字` 識別碼，不以一般姓名關鍵字任意排除真實參與者。
- 抽選方式：使用瀏覽器加密亂數執行 Fisher–Yates 隨機排序。
- 得獎名額：答題者達 20 位時抽出 20 位；不足 20 位時全部列為得獎者。
- 抽獎頁僅允許主持人授權名單內的 Google 帳號進入。

## 點數說明

抽獎頁依據提供的福利點數說明呈現：

`集團點 1 點 = 小樹點 1 點 = 新臺幣 1 元`

抽獎頁僅以重點文字介紹福利點數概念，不嵌入或提供完整 PDF。

## 音效

抽獎音樂與得獎音效採 Web Audio 即時產生，不需要額外 MP3 檔。瀏覽器依規定必須由使用者點擊「開始抽獎」後才能播放聲音；右上角可調整音量或靜音。若瀏覽器拒絕播放，抽獎流程仍會正常完成。
